#include <stdio.h>
int main()
{
  float length = 10.0;
  float breadth = 20.0;
  float area = length * breadth;
  printf("the area of the triangle with length = %f and breadth is %f is %f\n",length,breadth,area);
  return 0;
}
